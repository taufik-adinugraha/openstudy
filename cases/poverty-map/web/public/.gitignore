# View-models are generated by pipeline/export_web.py on the machine that has the data
# plane, and the deploy rsync deliberately never carries this directory — syncing it from a
# workstation has destroyed live data before. Rebuild with `make export`.
data/
