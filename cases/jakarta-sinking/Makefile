# Phase 2 — Jakarta Is Sinking (static snapshot case)
# Spec: TBD (in preparation)

PY := uv run python pipeline

.PHONY: rebuild validate export

rebuild:
	@echo "TODO: ingest → velocity → fuse → validate → export (see spec)"

validate:
	@echo "TODO: gate G-C1 (hotspot velocity vs published rates)"

export:
	@echo "TODO: web view-models"
