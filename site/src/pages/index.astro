---
// Landing page skeleton — lab chrome tokens (graphite + spectral cyan).
// Design contract: plan §3 — the landing stays neutral so case hues do the
// talking; the portfolio IS the demos (lead with playable work, no copy walls).
import "../../../shared/design/tokens.css";
---
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Demo Lab</title><!-- TODO D8: final lab name -->
  </head>
  <body>
    <main style="max-width: var(--content-width); margin: 0 auto; padding: var(--space-6) var(--space-4);">
      <p class="stamp" style="color: var(--accent);">DEMO LAB — WORKING NAME (D8 OPEN)</p>
      <h1>Advanced analytics, end to end, on data anyone can check.</h1>
      <p style="color: var(--ink-muted); max-width: var(--prose-width);">
        Case tiles go here — each links to an independently deployed demo.
        Week-5 build slot; see build plan §6.
      </p>
      <!-- TODO week 5: case tile grid (nightlights, trade), each tile carrying
           its own data-case token scope; CTA per D1/D8. -->
    </main>
  </body>
</html>
